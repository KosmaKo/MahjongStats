# MahjongStats Environment Variables
# Copy this file to .env and fill in your secrets
# DO NOT commit .env to source control!

# API Configuration
# Note: Bearer token is entered by user in the app UI, not stored in config
MahjongTracker__ApiUrl=URL

# Database (optional - only needed if not using default SQL Server)
# ConnectionStrings__DefaultConnection=connection_string_here

# Environment
ASPNETCORE_ENVIRONMENT=Production
