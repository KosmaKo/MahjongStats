@page "/refresh"
@using MahjongStats.Models
@using MahjongStats.Services
@inject IMahjongTrackerService MahjongService
@inject IGameFilterService FilterService
@inject IDatabaseService DatabaseService
@inject IConfiguration Configuration
@rendermode InteractiveServer

<PageTitle>Refresh Data - Mahjong Stats</PageTitle>

<div class="container-fluid mt-4">
    <h1>🔄 Refresh Data</h1>
    <p class="text-muted">Sync your Mahjong game data with the API.</p>

    <!-- Status Section -->
    <div class="card mb-4">
        <div class="card-body">
            <h5 class="card-title">Database Status</h5>
            @if (cachedGameCount > 0)
            {
                <div class="alert alert-success">
                    <strong>✓ Database is populated</strong>
                    <br/>
                    <small>Games in database: @cachedGameCount</small>
                    <br/>
                    <small>Rounds in database: @cachedRoundCount</small>
                </div>
            }
            else
            {
                <div class="alert alert-warning">
                    <strong>⚠ Database is empty</strong>
                    <br/>
                    <small>Sync data below to populate the database</small>
                </div>
            }
        </div>
    </div>

    <!-- Bearer Token Input -->
    <div class="card mb-4">
        <div class="card-header bg-primary text-white">
            <h5 class="card-title mb-0">Bearer Token</h5>
        </div>
        <div class="card-body">
            <div class="input-group">
                <input type="text" class="form-control" @bind="bearerToken" placeholder="Paste your bearer token from mahjongtracker.com" disabled="@isLoading">
            </div>
            @if (!string.IsNullOrEmpty(tokenError))
            {
                <div class="alert alert-danger mt-3">@tokenError</div>
            }
        </div>
    </div>

    <!-- Sync Actions Section -->
    <div class="card mb-4">
        <div class="card-header bg-primary text-white">
            <h5 class="card-title mb-0">Sync Actions</h5>
        </div>
        <div class="card-body">
            <p>Choose an action to sync your game data:</p>
            
            <!-- Button Group -->
            <div style="display: flex; flex-direction: column; gap: 1rem;" class="mb-4">
                <!-- Get New Games -->
                <div>
                    <button class="btn btn-success" style="width: 100%; padding: 0.75rem;" @onclick="GetNewGames" disabled="@(isLoading || string.IsNullOrEmpty(bearerToken))">
                        @if (isLoading && currentStep == "newGames")
                        {
                            <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                            <span>Syncing New Games...</span>
                        }
                        else
                        {
                            <span>📥 Get New Games</span>
                        }
                    </button>
                    <small class="text-muted d-block mt-2">Fetches any games you don't have yet and syncs their rounds</small>
                    @if (newGamesMessage != null)
                    {
                        <div class="alert @newGamesMessage.IsError mt-2 mb-0">@newGamesMessage.Text</div>
                    }
                </div>

                <!-- Refresh Games Since Date -->
                <div>
                    <button class="btn btn-warning" style="width: 100%; padding: 0.75rem;" @onclick="RefreshGamesSince" disabled="@(isLoading || string.IsNullOrEmpty(bearerToken))">
                        @if (isLoading && currentStep == "refreshSince")
                        {
                            <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                            <span>Refreshing Games...</span>
                        }
                        else
                        {
                            <span>🔄 Refresh Games Since</span>
                        }
                    </button>
                    <div class="mt-2">
                        <input type="date" class="form-control" @bind="refreshSinceDate" disabled="@(isLoading || string.IsNullOrEmpty(bearerToken))">
                    </div>
                    <small class="text-muted d-block mt-2">Removes all games created after the selected date and re-fetches them from the API</small>
                    @if (refreshSinceMessage != null)
                    {
                        <div class="alert @refreshSinceMessage.IsError mt-2 mb-0">@refreshSinceMessage.Text</div>
                    }
                </div>

                <!-- Get Missing Rounds -->
                <div>
                    <button class="btn btn-info" style="width: 100%; padding: 0.75rem;" @onclick="GetMissingRounds" disabled="@(isLoading || string.IsNullOrEmpty(bearerToken) || gamesMissingRounds <= 0)">
                        @if (isLoading && currentStep == "missingRounds")
                        {
                            <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                            <span>Fetching Rounds (@currentRoundFetched/@gamesMissingRounds)...</span>
                        }
                        else
                        {
                            <span>📊 Get Missing Rounds</span>
                        }
                    </button>
                    @if (gamesMissingRounds > 0)
                    {
                        <div class="alert alert-warning mt-2 mb-0">
                            <strong>⚠ @gamesMissingRounds game@(gamesMissingRounds != 1 ? "s" : "") missing rounds</strong>
                            @if (missingGameIds.Count > 0)
                            {
                                <br/>
                                <small>Game IDs: @string.Join(", ", missingGameIds)</small>
                            }
                        </div>
                    }
                    else if (gamesMissingRounds == 0 && cachedGameCount > 0)
                    {
                        <div class="alert alert-success mt-2 mb-0">
                            <strong>✓ All games have their rounds</strong>
                        </div>
                    }
                    <small class="text-muted d-block mt-2">Fetches rounds only for games that don't have them yet</small>
                    @if (missingRoundsMessage != null)
                    {
                        <div class="alert @missingRoundsMessage.IsError mt-2 mb-0">@missingRoundsMessage.Text</div>
                    }
                </div>
            </div>

            <!-- Throttle Delay Setting -->
            <div class="mb-3">
                <label class="form-label">Throttle Delay (milliseconds)</label>
                <div class="input-group" style="max-width: 200px">
                    <input type="number" class="form-control" @bind="throttleDelay" min="100" step="100" disabled="@isLoading">
                    <span class="input-group-text">ms</span>
                </div>
                <small class="text-muted">Delay between API requests to avoid rate limiting. Recommended: 100-500ms</small>
            </div>
        </div>
    </div>

    <!-- Summary -->
    <div class="card bg-light">
        <div class="card-body">
            <h5 class="card-title">Summary</h5>
            <ul>
                <li><strong>Games in database:</strong> @cachedGameCount</li>
                <li><strong>Rounds in database:</strong> @cachedRoundCount</li>
                <li><strong>Games missing rounds:</strong> @gamesMissingRounds</li>
            </ul>
        </div>
    </div>
</div>

@code {
    private class Message
    {
        public string Text { get; set; }
        public bool IsError { get; set; }
        public string AlertClass => IsError ? "alert-danger" : "alert-success";
    }

    private string bearerToken = "";
    private string tokenError = "";

    private bool isLoading = false;
    private string currentStep = "";

    private int throttleDelay = 100;
    private DateTime refreshSinceDate = DateTime.Today;
    private int gamesMissingRounds = 0;
    private int currentRoundFetched = 0;
    private List<string> missingGameIds = new();

    private int cachedGameCount = 0;
    private int cachedRoundCount = 0;

    private Message newGamesMessage;
    private Message refreshSinceMessage;
    private Message missingRoundsMessage;

    protected override async Task OnInitializedAsync()
    {
        // Load bearer token from config if available
        var token = Configuration["MahjongTracker:BearerToken"];
        if (!string.IsNullOrEmpty(token) && token != "YOUR_BEARER_TOKEN_HERE")
        {
            bearerToken = token;
        }

        await UpdateCacheStats();
    }

    private async Task UpdateCacheStats()
    {
        try
        {
            var allGames = await DatabaseService.GetAllGamesAsync();
            cachedGameCount = allGames.Count;
            
            var allRounds = await DatabaseService.GetAllRoundsAsync();
            cachedRoundCount = allRounds.Values.Sum(r => r.Count);
            
            // Check for missing rounds
            var gamesNeedingRounds = await MahjongService.GetGamesNeedingRoundsAsync();
            gamesMissingRounds = gamesNeedingRounds.Count;
            
            // Get up to first 5 game IDs missing rounds
            missingGameIds = gamesNeedingRounds.Take(5).ToList();
        }
        catch
        {
            cachedGameCount = 0;
            cachedRoundCount = 0;
            gamesMissingRounds = 0;
            missingGameIds = new();
        }
    }

    private async Task GetNewGames()
    {
        if (string.IsNullOrEmpty(bearerToken))
        {
            tokenError = "Bearer token is required";
            return;
        }

        isLoading = true;
        currentStep = "newGames";
        newGamesMessage = null;

        try
        {
            // Fetch new games
            var newGames = await MahjongService.FetchAndSyncNewGamesAsync(bearerToken);
            
            if (newGames.Any())
            {
                newGamesMessage = new Message { Text = $"✓ Synced {newGames.Count} new game(s)", IsError = false };
            }
            else
            {
                newGamesMessage = new Message { Text = "No new games found", IsError = false };
            }

            await UpdateCacheStats();
        }
        catch (Exception ex)
        {
            newGamesMessage = new Message { Text = $"Error: {ex.Message}", IsError = true };
        }
        finally
        {
            isLoading = false;
            currentStep = "";
        }
    }

    private async Task RefreshGamesSince()
    {
        if (string.IsNullOrEmpty(bearerToken))
        {
            tokenError = "Bearer token is required";
            return;
        }

        isLoading = true;
        currentStep = "refreshSince";
        refreshSinceMessage = null;

        try
        {
            var refreshedGames = await MahjongService.SyncGamesFromDateAsync(refreshSinceDate, bearerToken);
            
            if (refreshedGames.Any())
            {
                refreshSinceMessage = new Message { Text = $"✓ Refreshed {refreshedGames.Count} game(s) since {refreshSinceDate:yyyy-MM-dd}", IsError = false };
            }
            else
            {
                refreshSinceMessage = new Message { Text = $"No games found after {refreshSinceDate:yyyy-MM-dd}", IsError = false };
            }

            await UpdateCacheStats();
        }
        catch (Exception ex)
        {
            refreshSinceMessage = new Message { Text = $"Error: {ex.Message}", IsError = true };
        }
        finally
        {
            isLoading = false;
            currentStep = "";
        }
    }

    private async Task GetMissingRounds()
    {
        if (string.IsNullOrEmpty(bearerToken))
        {
            tokenError = "Bearer token is required";
            return;
        }

        isLoading = true;
        currentStep = "missingRounds";
        missingRoundsMessage = null;

        try
        {
            // Sync all games missing rounds
            var (syncedGameIds, totalRounds) = await MahjongService.SyncAllMissingRoundsAsync(bearerToken, throttleDelayMs: throttleDelay);
            
            if (!syncedGameIds.Any())
            {
                missingRoundsMessage = new Message { Text = "All games already have their rounds", IsError = false };
            }
            else
            {
                missingRoundsMessage = new Message { Text = $"✓ Fetched {totalRounds} rounds for {syncedGameIds.Count} game(s)", IsError = false };
            }

            await UpdateCacheStats();
        }
        catch (Exception ex)
        {
            missingRoundsMessage = new Message { Text = $"Error: {ex.Message}", IsError = true };
        }
        finally
        {
            isLoading = false;
            currentStep = "";
        }
    }
}
